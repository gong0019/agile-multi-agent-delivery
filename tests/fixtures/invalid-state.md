# This file is intentionally invalid for testing

## Missing Frontmatter

This state file has no YAML frontmatter block.
It should fail validation because it is missing all required fields.

There is no skill_version, no iteration_version, no acceptance_criteria.
