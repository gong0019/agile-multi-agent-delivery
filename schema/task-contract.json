{
  "$schema": "https://json-schema.org/draft/2020-12/schema",
  "title": "Task Contract",
  "description": "Schema for delegating bounded work slices to subagents. Each contract is derived from the current State Ledger and passed as the subagent's sole context.",
  "type": "object",
  "required": [
    "task_id",
    "round_version",
    "objective",
    "in_scope",
    "out_of_scope",
    "files_allowed",
    "files_avoid",
    "must_respect",
    "expected_deliverable",
    "stop_when",
    "escalate_if"
  ],
  "properties": {
    "task_id": {
      "type": "string",
      "pattern": "^TASK-[0-9]+$",
      "description": "Stable unique identifier for this delegated task"
    },
    "round_version": {
      "type": "string",
      "pattern": "^v[0-9]+$",
      "description": "Version of the iteration round this contract was created in"
    },
    "phase": {
      "type": "string",
      "enum": [
        "INIT",
        "REQUIREMENTS_DRAFTING",
        "REQUIREMENTS_REVIEW",
        "REQUIREMENTS_CONFIRMED",
        "PM_DECOMPOSITION",
        "BUILDING",
        "INTEGRATION_CHECK",
        "TESTING",
        "COMPLETE"
      ],
      "description": "Pipeline phase this contract belongs to"
    },
    "agent_role": {
      "type": "string",
      "enum": [
        "product-owner",
        "challenger",
        "project-manager",
        "builder",
        "tester",
        "integrator"
      ],
      "description": "Role of the agent receiving this contract"
    },
    "parallel_group": {
      "type": "string",
      "description": "Tag identifying which agents were spawned in the same batch, e.g. 'requirements-round-1'"
    },
    "objective": {
      "type": "string",
      "minLength": 1,
      "description": "Single sentence describing what this task must achieve"
    },
    "in_scope": {
      "type": "array",
      "minItems": 1,
      "items": {
        "type": "string"
      },
      "description": "Work items explicitly allowed"
    },
    "out_of_scope": {
      "type": "array",
      "minItems": 1,
      "items": {
        "type": "string"
      },
      "description": "Work items explicitly forbidden"
    },
    "files_allowed": {
      "type": "array",
      "minItems": 1,
      "items": {
        "type": "string"
      },
      "description": "File paths or globs the subagent may read or modify"
    },
    "files_avoid": {
      "type": "array",
      "items": {
        "type": "string"
      },
      "description": "File paths or globs the subagent must not touch"
    },
    "must_respect": {
      "type": "array",
      "items": {
        "type": "string"
      },
      "description": "Contracts, constraints, or rules the subagent must honor"
    },
    "contracts": {
      "type": "array",
      "minItems": 0,
      "items": {
        "type": "object",
        "required": ["contract_id", "role"],
        "properties": {
          "contract_id": {
            "type": "string",
            "pattern": "^C-[0-9]+$",
            "description": "Reference to a Contract Spec produced during PM decomposition"
          },
          "role": {
            "type": "string",
            "enum": ["provider", "consumer"],
            "description": "Whether this Builder provides or consumes the interface"
          }
        }
      },
      "description": "Cross-Slice Interface contracts this Builder must comply with. Each references a Contract Spec defined in the PM Decomposition Plan. May be empty only if this slice has zero cross-slice interfaces."
    },
    "expected_deliverable": {
      "type": "string",
      "minLength": 1,
      "description": "Concrete output the subagent must produce, in Agent Return format"
    },
    "stop_when": {
      "type": "string",
      "minLength": 1,
      "description": "Clear finish line that ends this task"
    },
    "escalate_if": {
      "type": "string",
      "minLength": 1,
      "description": "Decision point or blocker that requires Orchestrator intervention"
    },
    "design_constraints": {
      "type": "object",
      "description": "Frontend design constraints. Required when files_allowed includes UI files (pages, components, views, templates, stylesheets).",
      "properties": {
        "css_framework": {
          "type": "string",
          "description": "e.g. 'Tailwind CSS v3', 'CSS Modules', 'styled-components'"
        },
        "component_library": {
          "type": "string",
          "description": "e.g. 'shadcn/ui', 'Ant Design 5', 'Material UI', 'none'"
        },
        "icon_library": {
          "type": "string",
          "description": "e.g. 'lucide-react', 'heroicons', '@ant-design/icons'"
        },
        "reference_pages": {
          "type": "array",
          "items": { "type": "string" },
          "description": "2-3 file paths to existing pages that exemplify the desired visual style"
        },
        "layout_pattern": {
          "type": "string",
          "description": "e.g. 'sidebar+content', 'full-width card grid', 'centered form'"
        },
        "design_tokens": {
          "type": "object",
          "description": "Key visual tokens extracted from existing code",
          "properties": {
            "primary_color": { "type": "string" },
            "border_radius": { "type": "string" },
            "spacing_scale": { "type": "string" },
            "font_family": { "type": "string" }
          }
        },
        "must_not": {
          "type": "array",
          "items": { "type": "string" },
          "description": "e.g. 'use inline styles', 'introduce new CSS frameworks', 'use emoji as icons'"
        }
      }
    }
  }
}
